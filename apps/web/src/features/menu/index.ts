/**
 * Menu & pricing feature barrel. The integrator imports these page components and wires
 * them into the route registry in Phase 2 (see this slice's final report for intended
 * paths, labels, and roles).
 */
export { MenuPage } from './MenuPage';
export { PricingAdminPage } from './PricingAdminPage';
