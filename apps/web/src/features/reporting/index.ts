/**
 * Reporting feature barrel (TDD §13, §17.2). The integrator imports `ReportsPage` from
 * here to register the `/reports` route. The chart components are reusable building
 * blocks scoped to this feature and are not part of the public surface.
 */
export { ReportsPage } from './ReportsPage';
